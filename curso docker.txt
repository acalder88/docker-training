docker pull openjdk:8

docker images

docker rmi {container_id}

docker run openjdk:8 echo "Hello World"

docker ps

docker ps -all

docker ps -qa

docker run -it openjdk:8 /bin/bash
echo "Hello World" > /tmp/test.txt
exit

docker ps -qa

docker start --attach -i {container_id}
echo "Hello World" > /tmp/test2.txt
exit

docker ps -qa

docker start --attach -i {container_id}
ctrl+p ctrl+q

Explicar antes del 

docker stop {container_id}
docker kill {container_id}
docker rm {container_id}


docker diff {container_id}
Symbol	Description
A	A file or directory was added
D	A file or directory was deleted
C	A file or directory was changed

docker commit c83fb44c298d myimage:mytag

docker images --help

/////////////////Pruebas//////////////////
docker run -d openjdk:8 /bin/bash
docker run -di openjdk:8 /bin/bash
docker exec -it {container_id} /bin/bash
exit
se murio o no?
///////////////////////////////////////

docker top {contianer_id}
docker stats {container-id}

echo "test all" > /tmp/test3.txt
docker cp /tmp/test3.txt 002a70346c9c:/tmp/test3.txt

docker push imagen

Ejercicio uno 
Ejecutar la imagen openjdk:8 interactivamente
Copiar el jar success.jar al contenedor
entrar al contenedor y ejecutar java -jar success.jar

Mostrar lesson2

docker images
docker tag {image_id} repo:tag

curl localhost:8080/api/talker?message=Hola

Crear imagen que use volumenes
docker run -it --rm -v "$PWD":/usr/src/myapp -w /usr/src/myapp python:3.6-alpine3.6 python test.py


Ejercicio dos
Crear un docker file que use el jar success.jar y lo ejecute
Taggear la imagen y hacerle push

FROM openjdk:8
COPY success.jar ./
ENTRYPOINT ["java", "-jar", "success.jar"]
docker build .
docker run 5b5a0751b31ecd 

//////////////////////////Compose/////////////////
explicar arquitectura
mostar el compose
docker-compose up

http://localhost:8080/api/container?message=holi


docker kill consumer container
docker-compose restart consumer

////////////orquestador////////////////////////

Mostrar el talker






